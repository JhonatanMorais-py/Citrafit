# Copie este arquivo para .env e preencha apenas no ambiente do backend.
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sua-chave-service-role
APP_PORT=3000
APP_HOST=127.0.0.1
# In HTTPS deployment, set NODE_ENV=production for Secure Web cookies.
NODE_ENV=development
NATIVE_ORIGINS=https://localhost,capacitor://localhost
# MOBILE_API_URL is public build configuration, supplied separately to npm run build.
